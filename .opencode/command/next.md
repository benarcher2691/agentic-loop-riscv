---
description: Do the next unchecked task in TASKS.md (one loop iteration, by hand)
agent: loop
---

Read AGENTS.md, then do exactly one iteration of the workflow it describes:
run `make check`, take the first unchecked task in TASKS.md, write its bench, implement it,
get `make check` green, tick the task, note progress in PROGRESS.md, commit, and stop.

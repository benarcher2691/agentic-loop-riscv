# Tasks

Ordered. The loop agent takes the **first unchecked** task each iteration. Every task ends with
`make check` green (all benches PASS, synth without latches, fits the hx1k at 12 MHz) and a commit.
Architecture target, so no task requires rewriting an earlier one: `SOC` (clock, reset, IO) →
`Processor` (CPU) + `Memory` (RAM/ROM), plus small pure modules `Decoder`, `ALU`, `Clockworks`.

# Phase 2 — a computer, not just a core



Same rules. The SoC currently has 1 KB RAM, UART transmit only, and no way to read time.

# Phase 3 — hardware in the loop































































The board becomes a test fixture the loop can drive over the UART. The monitor and the test programs































are built and proven in simulation by the loop (the serial models in the benches already speak both































directions); the host-side tool and `make hwcheck` are supervisor work (they need the board to test).

# Phase 4 — visual verification (parked; not needed until the SoC has a visual peripheral)































































































































































































































































































































































































































































































































Idea, not yet a task: a fixed webcam over the board as a verifier for outputs that have no register to































































































































































































































































read back (OLED, LED matrix, VGA). Capture a frame (`imagesnap`/`ffmpeg` — one extra tool), then either































































































































































































































































pixel-compare fixed regions against an image rendered by the simulation (deterministic, alignment-































































































































































































































































sensitive) or hand the frame to a vision-capable model with a yes/no question (robust, fuzzy). For the































































































































































































































































five LEDs the UART monitor (phase 3) is the better sensor; revisit this when the first visual peripheral































































































































































































































































lands. Not a `- [ ]` item on purpose — the loop must not pick it up.























































































































































































































































































































































































































































































































































































































































































































































































# Phase 5 — audit fixes (2026-08-29)















Findings from the four-reviewer audit (`docs/audit-2026-08-29.md`; IDs A1–A23 there). Same







rules as ever: regression FIRST (demonstrate, then fix), one task per session, `make check`







fully green — note it now includes the `hwreset` stage — and both LC budgets hold at 1150.

- [x] **T2: Halt on bad data addresses (A1 major, A2).** *(Done 2026-08-29. `rtl/processor.v`: `badAddr = misalign | outOfRange` computed combinationally on `aluOut` during EXECUTE — misalign: LW/SW need `addr[1:0]==00`, LH/LHU/SH need `addr[0]==0` (one term, `funct3[0]&~funct3[1]`, covers both), bytes never; outOfRange: bit 22 clear AND (`addr[12:11]==11` i.e. ≥0x1800 in the 13-bit space, OR any bit ≥13 set i.e. the alias region). Gated into `mem_rstrb` and `mem_wmask` (an ungated RX read would clear rxAvail, an ungated store would commit) plus a halt arm in EXECUTE ordered after the SYSTEM halt and before the isLoad arm, so `ldOff` is never latched and rd is never written. `tb/badaddr_tb.v` (188 checks): 29 case routines reached through a JAL trampoline at MEM[0] (halt is permanent, reset recovers, registers persist) — 19 bad cases (LW/SW at 0x101/2/3, LH/LHU/SH at 0x101, aligned LB/LBU/LH/LW/SW/SB at 0x1800, alias loads 0x2000/0x4000/0x80000000, alias store 0x2440, IO-space misaligned LW 0x400002 / SH 0x400001) each get 6 universal halt checks (state=EXECUTE, PC frozen, PC still frozen 25 cycles later, halt permanent, rstrb=0, wmask=0) + rd/rs2 sentinel preserved + poison marker after the access never executed + memory canaries untouched (word at 0x100, aliased word 272, trampoline word 0); 10 boundary-good cases (LW 0x17FC, LH/LHU 0x17FE, LB/LBU 0x17FF, LW 0x0000, SW 0x17F8, aligned IO LW 0x400480 / SW 0x4004C0 / LW 0x402000) prove execution reaches the EBREAK with correct load data. The bench memory model reproduces the pre-fix symptoms exactly (X reads, vanishing writes) — fault-injection pattern worth reusing. `tb/hwprogs_tb.v` ldst: misaligned LH/LHU 1/3 and SH 13/15 removed, replaced with equal-strength aligned coverage (LH/LHU at 4/6 cover the second pattern word; SH 20/22 with a second value 0x8765, sign bit set, both lane pairs); expectations recomputed from the pattern, encoding cross-checks re-indexed (SB→MEM[75], SH→MEM[95]); result count unchanged (27). Budgets: unflattened 1147 → 1158 (+11 for the comparators and halt arm), pnr 1141 → 1163, LC_BUDGET 1150 → 1180 raised by Ben (99e5ede) as deliberate safety logic; Fmax 38.45 → 34.63 MHz (2.9× the 12 MHz requirement); equiv clean; 15635 → 15823 checks in 19 benches.)* In EXECUTE, a load/store must halt the CPU (the same permanent state-freeze as SYSTEM: PC frozen, `rd` not written, no strobe, no wmask) when the effective address is **misaligned** (LW/SW with `addr[1:0]!=0`; LH/LHU/SH with `addr[0]!=0`; byte accesses are always aligned) or **out of range** (bit 22 clear and address ≥ 0x1800 — today such loads return X and stores vanish). Write the regression bench first (`tb/badaddr_tb.v`, sim-only): each bad case via a small program; assert PC frozen for ≥20 cycles, `rd` unchanged, no memory/IO side effect; plus boundary-good cases (LW at 0x17FC, LH at 0x17FE). Then update `tb/hwprogs_tb.v`: it currently **pins the misaligned quirks as expected** (`tb/hwprogs_tb.v:330-334`) — remove those cases from the ldst *hardware* program (hardware programs must remain halt-free) and replace with aligned coverage of equal strength. Budgets must hold (T1 freed the room); watch Fmax.

- [x] **T3: Exact-match IO decode (A3).** *(Done 2026-08-29. `rtl/soc.v`: the four per-bit decodes replaced by exact equality on `mem_addr[5:2]` — `ioLeds` (0001, shared read+write), `ioUartW` (0010 **and `mem_wmask == 4'b1111`**: the UART data port accepts full-word stores only, so the monitor's byte-wise W can walk across IO words without firing the transmitter — this is also the T4(b)/M4 kill, already pinned by `tb/iowalk_tb.v`), `ioUartS` (0100), `ioUartRx` (1000); `ioLedsW = ioLeds & mem_wmask[0]`; the unused `storeNow` wire deleted. Read path: explicit LEDS arm (`ioLeds ? {27'd0, ledReg}`) and the default arm is now `32'd0` — unmapped IO reads return 0 with no side effect, so `rxAvail` survives loads at 0x400024/28/30. `tb/ioexact_tb.v` (47 checks): one CPU program — bad store to 0x40000C (pre-fix it latched LEDs=0x1f AND sent a 0xFF frame), unmapped loads 0x400000/0x400014/0x40000C/0x400024×2/0x400018, an RX byte timed to complete inside the TX-busy window of two 0x55 frames so avail is pending across the unmapped loads without polling RX (a poll read would consume it), real RX read returns 0x15A; event-driven TXD recorder pins exactly two frames. `tb/iowalk_tb.v` (4 checks): monitor `W 0x400004 len=8` walks 0x400004..B — LEDs from byte 0, TXD = 12-byte banner + exactly one `K` (verified to FAIL pre-fix: 0xDD frame, txdN=14). Budgets: unflattened 1158 → 1165 (SOC 57 → 65), pnr 1163 → 1175, Fmax 34.63 → 33.20 MHz; equiv clean; 15823 → 15874 checks in 21 benches.)* The four IO words decode by equality on `mem_addr[5:2]` (`0001`→LEDS 0x400004, `0010`→UART data 0x400008, `0100`→UART status 0x400010, `1000`→RX 0x400020). Any other IO offset: reads return `32'd0` with **no side effects** (in particular they must not clear `rxAvail`), writes are dropped. Regressions first: a store to `0x40000C` changes neither the LEDs nor starts a TX frame; a load of `0x40000C` returns 0 and leaves a pending RX byte's `avail` set; a monitor `W 0x400004 len=8` sets the LEDs from byte 0 and produces nothing on TXD except the `K` reply; a load of an unmapped IO offset no longer returns the LED word.

- [x] **T4: UART contracts (A4 major, A5 major, A12, A21).** *(Done 2026-08-29. Engineering finished by the iteration-4 handoff (tree TIMEOUT), verified and committed by this session. `tb/rxoverrun_tb.v` (18 checks): CPU parked at a planted EBREAK so forced strobes are deterministic; overrun = last-writer-wins (0x11 then 0x22 unread → data=0x22, single read returns {avail=1,0x22}, avail clears, 0x33 still works); set-vs-clear race pinned by arming the forced strobe on the edge AFTER uartRxValid rises (hold-past-the-edge fix — the first cut released before the NBA update and sampled one cycle late), with a `sawRace` witness so it cannot pass vacuously; racing read returns the pre-race avail=0 but rxAvail ends SET and a follow-up read delivers the byte — nothing lost. Policy + host-must-throttle documented at the `rxAvail` block. `tb/txbusy_tb.v` (39 checks): uploaded G routine at 0x600 does SW 'A' then SW 'B' back-to-back to 0x400008; hierarchical witnesses `sawSw1Ready` (PC=0x60C, uartReady=1) and `sawSw2Busy` (PC=0x610, uartReady=0) prove the busy-drop really happened; TXD = banner(12)+K+'A'+K+"RV32" exactly (19 frames, event-driven recorder, no strays), 'B' checked absent from all 19, later V bytes transmit. Routine cross-checked word-by-word against a hand encoder (LUI 0x004002B7 trap avoided). Comments: A12 uartValid hold rationale rewritten (the hold is the valid/ready handshake; the old per-bit-shift claim was wrong — the reload pins the counter's top bit while idle), A16 stale "buggy SRLI" claim corrected in tb/io_tb.v (the macro's encoding is exactly SRLI). RTL comments-only: budgets unchanged from T3 (1165 unflattened, 1175 pnr, 33.20 MHz), equiv clean; 15874 → 15931 checks in 23 benches.)* (a) Pin RX overrun as last-writer-wins: send two back-to-back bytes with no read between; a single read returns the SECOND byte with `avail` set before and clear after; a subsequent byte still works. Pin the set-vs-clear race deterministically: force/release the bus (`mem_rstrb`/`mem_addr`, the technique already used in `tb/uart_rx_tb.v`) so the read strobe lands on the exact `uartRxValid` cycle; assert the completing byte is preserved (set wins). Document the policy and the host-must-throttle contract in a comment at the `rxAvail` block. (b) Kill mutation M4 — SPEC AMENDED after T3: byte stores to the UART data register are now inert (`ioUartW` requires `mem_wmask == 4'b1111`), so the monitor's byte-wise `W` can NEVER reach the transmitter and a test through that path is vacuous. Instead, drive a full-word `SW` to 0x400008 while TX is busy from CPU code: upload a `G` routine that does `SW` to the UART data register twice back-to-back (the second lands while the first frame is in flight), then returns. The bench must (1) hierarchically confirm the second write cycle really occurred with `uartReady` low (so the test cannot pass vacuously), and (2) assert the TXD stream contains exactly the first byte's frame — the busy-dropped byte never appears — and a later normal byte does. (c) Comments only, no behaviour change: correct the wrong `uartValid` hold rationale (`rtl/soc.v:115-121`, audit A12) and delete the stale SRLI claim in `tb/io_tb.v` (A16).

- [ ] **T5: Coverage batch (A7/A19, A9, A11, A13/A22, A23).** (a) `tb/cycle_tb.v`: deposit `cycles=32'h7FFF_FFFE, cycleh=5`, step 3 edges, assert `cycleh` stays 5 (kills the `&cycles[30:0]` mutant); one CSR read whose EXECUTE lands exactly on the 0xFFFF_FFFF→0 wrap cycle; assert ECALL (`32'h00000073`), CSRRC and one CSRRxI encoding all halt permanently. (b) `rtl/uart_rx.v`: size the bit counter with `$clog2(BIT_CLKS)` — no behaviour change. (c) `tb/monitor_tb.v`: `W addr 0` → `K`, memory untouched; `R addr 0` → zero data bytes then a live `V`; a split command (`W` + 2 address bytes, 50 bit-times of silence, then the rest) completes normally; a `G` routine that clobbers EVERY register except `x2` (including `x1` and `x5`) → `K`, live `V`, correct LEDs. (d) `ADD x0,xN,xM` and `AUIPC x0,…` leave `x0` = 0 (one-liners in the existing benches). (e) One backward branch or JAL whose target crosses the 8 KB PC wrap, pinned mod-8192 (place the code high via `memPC`). (f) Store to the word at PC+4, then execute it: pin that the newly stored instruction executes (3-state FSM, no prefetch).

- [ ] **T6: Equiv drives the SoC (A8/A20).** Extend `tools/equiv_tb.v`: add a serial `send_byte` task (copy `tb/monitor_tb.v`'s) driving the SAME `RXD` stimulus into both SOCs: the banner, then `V`, a 1-byte `W` of `0x15` to `0x400004`, an `R` of that word, and a small `G` (upload 2–3 instructions that write a word and `RET`). Run ~150–200k cycles but keep `make equiv` under ~90 s. Convert the vacuous prints into assertions: zero port mismatches (existing), a minimum TXD transition count (>200), at least one LED change, final `LEDS == 5'b10101` on both, and the decoded TXD byte streams identical between RTL and netlist (one serial receiver per DUT, byte-compare).

